
import * as path from 'path';

export const protoPath: string = path.normalize('../proto');
export const aiFile: string = protoPath + '/ai.proto';
export const stadiumFile: string = protoPath + '/stadium.proto';
